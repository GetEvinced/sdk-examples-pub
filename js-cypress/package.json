{
  "devDependencies": {
    "cypress": "^14.0.1"
  },
  "scripts": {
    "cy:open": "cypress run --browser chrome --headless",
    "cy-local-test": "npx cypress run --browser chrome"
  },
  "dependencies": {
    "@evinced/cypress-sdk": "^5.4.0"
  }
}
