# Gradle daemon JVM is picked up from JAVA_HOME (set by actions/setup-java in CI,
# or your local JAVA_HOME). The test toolchain in build.gradle independently pins
# test execution to JDK 17 regardless of what JDK runs the daemon.
